# 🚀 دليل ربط السيرفر والكلاينت على Vercel

## المشكلة الحالية
الكلاينت على `mur-ten.vercel.app` مش قادر يتصل بالسيرفر لأن:
1. ❌ VITE_API_URL مش مظبوط في Vercel
2. ❌ إعدادات CORS في السيرفر كانت محتاجة تحديث

## ✅ الحل (خطوة بخطوة)

### الخطوة 1️⃣: اعرف رابط السيرفر
1. روح على Vercel Dashboard: https://vercel.com/dashboard
2. اختر مشروع **السيرفر** (اللي فيه الـ backend)
3. انسخ الـ **Production URL** (مثال: `https://smoo-saas-server.vercel.app`)

### الخطوة 2️⃣: ضبط Environment Variables للكلاينت
1. روح على مشروع **الكلاينت** في Vercel (مشروع `mur-ten.vercel.app`)
2. اضغط على **Settings** → **Environment Variables**
3. أضف متغير جديد:
   - **Name**: `VITE_API_URL`
   - **Value**: رابط السيرفر اللي نسخته (مثال: `https://smoo-saas-server.vercel.app`)
   - **⚠️ مهم**: بدون `/api` في الآخر
4. اضغط **Save**

### الخطوة 3️⃣: ضبط Environment Variables للسيرفر
1. روح على مشروع **السيرفر** في Vercel
2. اضغط على **Settings** → **Environment Variables**
3. تأكد من وجود المتغيرات دي:
   - ✅ `MONGODB_URI`: رابط قاعدة البيانات (MongoDB Atlas)
   - ✅ `JWT_SECRET`: `almurqab_secret_key_2026`
   - ✅ `CLIENT_URL`: `https://mur-ten.vercel.app` (اختياري)

### الخطوة 4️⃣: Redeploy المشروعين
1. روح على مشروع **السيرفر** → **Deployments**
2. اضغط على آخر deployment → **Redeploy**
3. بعدين روح على مشروع **الكلاينت** → **Deployments**
4. اضغط على آخر deployment → **Redeploy**

---

## 📝 ملاحظات مهمة

### تحديث الـ .env المحلي (اختياري)
لو عايز تشتغل على المشروع محلياً، حدّث الملف `.env` في مجلد `client`:

```bash
# client/.env
VITE_API_URL=https://your-server-url.vercel.app
```

استبدل `your-server-url.vercel.app` برابط السيرفر الحقيقي.

### ⚠️ CORS مضبوط تلقائياً
الكود اللي عدلناه في `server/index.js` بيسمح تلقائياً لـ:
- ✅ `http://localhost:5173` (تطوير محلي)
- ✅ `https://mur-ten.vercel.app` (الكلاينت على Vercel)
- ✅ أي رابط تحطه في `CLIENT_URL`

---

## 🧪 اختبار الربط

بعد ما تخلص الخطوات، جرّب:

1. افتح: https://mur-ten.vercel.app
2. حاول تسجل دخول
3. لو شتغل → ✅ كل حاجة تمام!
4. لو لسه فيه مشكلة → افتح Console في المتصفح (F12) وابعتلي الـ error

---

## 🔍 كيف تتأكد إن السيرفر شغال؟

افتح رابط السيرفر في المتصفح (مثال: `https://your-server.vercel.app`)

**لو شغال صح**، هتشوف رسالة زي دي:
```json
{
  "status": "Server is running",
  "message": "🚀 Al Murqab Legal SaaS API",
  "database": {
    "status": "✅ Connected Successfully"
  }
}
```

**لو فيه مشكلة**، هتشوف error في `database.error`.

---

## ❓ مشاكل شائعة

### المشكلة: "Request failed with status code 404"
**الحل**: تأكد إن `VITE_API_URL` مظبوط في Vercel وإنك عملت Redeploy

### المشكلة: "CORS Policy Error"
**الحل**: تأكد إنك عملت Redeploy للسيرفر بعد تحديث `index.js`

### المشكلة: "MongoDB Connection Failed"
**الحل**: تأكد من `MONGODB_URI` في Environment Variables للسيرفر

---

## 📞 محتاج مساعدة؟

لو عملت كل الخطوات ولسه فيه مشكلة:
1. افتح Console في المتصفح (F12)
2. انسخ الـ error message الكامل
3. ابعته ونشوف المشكلة فين
