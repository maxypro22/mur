# نظام الـ SaaS لإدارة مجموعة المرقاب القانونية ⚖️

نظام متكامل لإدارة مكاتب المحاماة، الموكلين، القضايا، والسجلات المالية. مبني باستخدام React (Vite) و Node.js.

---

## �️ الدليل العربي للرفع والتشغيل (Deployment Guide)

هذا الدليل يشرح لك كيفية رفع المشروع من الصفر وحتى التشغيل النهائي.

### 1️⃣ الخطوة الأولى: رفع الكود على GitHub
لرفع المشروع على حسابك في GitHub:
1. قم بإنشاء مستودع (Repository) جديد على GitHub باسم `al-murqab-saas`.
2. في مجلد المشروع على جهازك، افتح الـ Terminal ونفذ الأوامر التالية:
   ```bash
   git init
   git add .
   git commit -m "إطلاق النسخة الأولى"
   git branch -M main
   git remote add origin https://github.com/ACCOUNT_NAME/al-murqab-saas.git
   git push -u origin main
   ```

---

### 2️⃣ الخطوة الثانية: قاعدة البيانات (MongoDB Atlas - مجاني)
نستخدم MongoDB Atlas للحصول على قاعدة بيانات سحابية مجانية:
1. قم بإنشاء حساب في [MongoDB Atlas](https://www.mongodb.com/cloud/atlas).
2. أنشئ "Cluster" جديد (اختر الخطة المجانية Free Tier).
3. من قسم "Database Access"، أنشئ مستخدم بكلمة مرور.
4. من قسم "Network Access"، أضف العنوان `0.0.0.0/0` للسماح بالاتصال من أي مكان.
5. اضغط على "Connect" واختر "Drivers" للحصول على رابط الاتصال (Connection String).

---

### 3️⃣ الخطوة الثالثة: الرفع على Vercel (الطريقة المجانية)
Vercel توفر استضافة مجانية ممتازة للمشاريع الشخصية:

#### أ- رفع الخادم (Backend - API)
1. اربط حساب Vercel الخاص بك بـ GitHub.
2. اختر مجلد `server` للرفع.
3. **أهم خطوة:** أضف متغيرات البيئة (Environment Variables) في إعدادات Vercel:
   - `MONGODB_URI`: رابط الاتصال الذي حصلت عليه من الخطوة الثانية.
   - `JWT_SECRET`: كلمة سر عشوائية لتشفير البيانات.
   - `NODE_ENV`: قم بتعيين قيمتها إلى `production`.

#### ب- رفع الواجهة (Frontend - Client)
1. اختر مجلد `client` للرفع.
2. في متغيرات البيئة (Environment Variables)، أضف:
   - `VITE_API_URL`: الرابط الذي حصلت عليه بعد رفع الخادم (مثلاً: `https://your-api.vercel.app/api`).

---

### 4️⃣ الخطوة الرابعة: الخيارات المدفوعة (للشركات)
إذا كنت ترغب في تشغيل النظام بشكل احترافي للعملاء:
1. **Hostinger / GoDaddy (VPS):** يمكنك شراء سيرفر خاص (VPS) واستخدام PM2 لتشغيل الخادم بشكل دائم خلف Nginx.
2. **Railway.app / Heroku:** خيارات مدفوعة أسهل في الإعداد وتوفر أداءً مستقراً جداً وقواعد بيانات مدمجة.
3. **Vercel Pro:** إذا كنت تحتاج لأداء أعلى، دعم فني، أو ربط أكثر من نطاق (Domain) مخصص.

---

## 📂 هيكلة المشروع
- `client/`: تطبيق الواجهة الأمامية (React).
- `server/api/`: الخادم والـ API (مصمم ليعمل كـ Serverless على Vercel).
- `server/models/`: نماذج البيانات (قواعد البيانات).
- `server/routes/`: مسارات النظام.

---

**تم التطوير لنظام SaaS مجموعة المرقاب القانونية.**
"# murqab" 
"# murqab" 
"# murqab" 
"# murlaw" 
