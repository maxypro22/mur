# 🚀 رفع السيرفر على Vercel - خطوة بخطوة

## المشكلة الحالية:
الكلاينت على `https://almurqab.vercel.app` مش قادر يلاقي السيرفر لأن السيرفر مش مرفوع أو على رابط مختلف.

---

## ✅ الحل: رفع السيرفر على Vercel

### الطريقة الأولى: من خلال Vercel Dashboard (الأسهل)

#### الخطوة 1️⃣: روح على Vercel
1. افتح https://vercel.com/dashboard
2. اضغط **Import Project** أو **Add New** → **Project**

#### الخطوة 2️⃣: اختر GitHub Repository
1. اختر الـ repository الخاص بك (مثال: `mur` أو `smoo-saas`)
2. اضغط **Import**

#### الخطوة 3️⃣: ⚠️ **مهم جداً** - إعدادات المشروع
بما إن عندك `client` و `server` في نفس الـ repo، لازم تحدد مجلد السيرفر:

في صفحة Configure Project:
```
Project Name: smoo-saas-server (أو أي اسم تختاره)

Framework Preset: Other

Root Directory: server  ← **مهم جداً!**

Build Command: (اتركه فاضي أو حط: echo "No build needed")

Output Directory: (اتركه فاضي)

Install Command: npm install
```

#### الخطوة 4️⃣: Environment Variables
قبل ما تضغط Deploy، اضغط على **Environment Variables** وأضف:

**⚠️ مهم جداً - لازم تضيف دول:**

| Name | Value | Examples |
|------|-------|----------|
| `MONGODB_URI` | رابط MongoDB Atlas | `mongodb+srv://user:pass@cluster.mongodb.net/almurqab` |
| `JWT_SECRET` | `almurqab_secret_key_2026` | (نفس القيمة) |
| `CLIENT_URL` | `https://almurqab.vercel.app` | (رابط الكلاينت) |
| `NODE_ENV` | `production` | (ثابت) |

**⚠️ لو ما عندكش MongoDB Atlas:**
1. روح https://cloud.mongodb.com
2. سجل دخول (أو أنشئ حساب مجاني)
3. اعمل Cluster جديد (Free Tier)
4. اضغط **Connect** → **Connect your application**
5. انسخ Connection String واستبدل `<password>` بكلمة السر الحقيقية

#### الخطوة 5️⃣: Deploy
اضغط **Deploy** وانتظر الـ deployment يخلص (حوالي 1-2 دقيقة)

#### الخطوة 6️⃣: اختبار السيرفر
بعد ما الـ deployment يخلص:
1. انسخ الـ Production URL (مثال: `https://smoo-saas-server-abc123.vercel.app`)
2. افتحه في المتصفح
3. لازم تشوف:
```json
{
  "status": "ALL DONE ✅",
  "message": "🚀 Al Murqab Legal SaaS API - Updated Successfully",
  "database": {
    "status": "✅ Connected Successfully"
  }
}
```

#### الخطوة 7️⃣: ربط الكلاينت بالسيرفر
بعد ما السيرفر يشتغل:
1. روح على مشروع **الكلاينت** في Vercel
2. اضغط **Settings** → **Environment Variables**
3. أضف (أو عدّل):
   - **Name**: `VITE_API_URL`
   - **Value**: رابط السيرفر اللي نسخته (مثال: `https://smoo-saas-server-abc123.vercel.app`)
   - **⚠️ بدون `/api` في النهاية!**
4. احفظ

#### الخطوة 8️⃣: Redeploy الكلاينت
1. روح **Deployments** في مشروع الكلاينت
2. اضغط على آخر deployment
3. اضغط **Redeploy**
4. انتظر يخلص

#### الخطوة 9️⃣: اختبار النهائي
1. افتح `https://almurqab.vercel.app`
2. جرب تسجل دخول
3. المفروض يشتغل! ✅

---

## 🔍 إزاي تتأكد إن كل حاجة تمام؟

### اختبار السيرفر:
افتح رابط السيرفر → لازم تشوف JSON فيه `"status": "ALL DONE ✅"`

### اختبار الكلاينت:
افتح Console في المتصفح (F12):
- لو فيه CORS error → راجع إعدادات CORS في `server/index.js`
- لو فيه 404 error → راجع `VITE_API_URL`
- لو فيه Network error → السيرفر مش شغال

---

## 📝 ملاحظات مهمة

### ⚠️ لو السيرفر والكلاينت في نفس Repository:
لازم تعمل **مشروعين منفصلين** على Vercel:
1. **مشروع 1**: Root Directory = `server`  
2. **مشروع 2**: Root Directory = `client`

### ✅ بعد ما تخلص:
1. السيرفر هيكون على: `https://[project-name].vercel.app`
2. الكلاينت على: `https://almurqab.vercel.app`
3. لازم تحط رابط السيرفر في `VITE_API_URL` للكلاينت

---

## 🆘 لو واجهت مشاكل:

### المشكلة: "Deployment Failed"
**الحل**: شوف الـ Build Logs في Vercel - غالباً بيكون فيه خطأ في Environment Variables

### المشكلة: "Database Connection Failed"
**الحل**: تأكد إن `MONGODB_URI` صحيح وإن MongoDB Atlas مسموح بالاتصال من Vercel (IP Whitelist: `0.0.0.0/0`)

### المشكلة: لسه Network Error
**الحل**: تأكد إن `VITE_API_URL` في الكلاينت بيشاور على رابط السيرفر الصحيح
