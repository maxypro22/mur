# 🔧 تشخيص مشكلة الاتصال بين الكلاينت والسيرفر

## ✅ خطوات التشخيص السريع

### الخطوة 1: تحقق من السيرفر على Vercel

1. افتح: https://vercel.com/dashboard
2. شوف كل المشاريع اللي عندك
3. **سؤال مهم:** كم مشروع عندك؟
   - ✅ **مشروعين** (واحد للكلاينت + واحد للسيرفر) = صح
   - ❌ **مشروع واحد** فقط = خطأ! لازم مشروعين منفصلين

### الخطوة 2: تأكد من رابط السيرفر

**للسيرفر** (لو موجود):
1. اضغط على مشروع السيرفر
2. شوف الـ Production URL
3. **انسخ الرابط بالضبط** (مثال: `https://smoo-saas-server-abc123.vercel.app`)
4. افتح الرابط في متصفح جديد
5. **لازم تشوف:**
   ```json
   {
     "status": "ALL DONE ✅",
     "message": "🚀 Al Murqab Legal SaaS API - Updated Successfully"
   }
   ```

**لو شفت Error 404 أو أي حاجة تانية:**
- معناها السيرفر مش شغال صح
- ممكن يكون في مشكلة في الـ deployment

### الخطوة 3: تأكد من Environment Variables

**في مشروع السيرفر:**
روح Settings → Environment Variables → تأكد من وجود:
- ✅ `MONGODB_URI` (يشاور على MongoDB Atlas، مش localhost)
- ✅ `JWT_SECRET`
- ✅ `CLIENT_URL` (اختياري)

**في مشروع الكلاينت:**
روح Settings → Environment Variables → تأكد من:
- ✅ `VITE_API_URL` = رابط السيرفر الحقيقي (بدون `/api` في النهاية)

### الخطوة 4: اختبر الاتصال

افتح Console في المتصفح (F12) على `https://almurqab.vercel.app` وشوف الـ errors:

**لو شفت:**
```
Network Error
```
**معناها:** الكلاينت مش قادر يلاقي السيرفر أصلاً
- تأكد إن `VITE_API_URL` صحيح
- تأكد إن السيرفر شغال فعلاً

**لو شفت:**
```
CORS policy error
```
**معناها:** السيرفر موجود بس مش بيسمح بالطلبات من الكلاينت
- تأكد إن `https://almurqab.vercel.app` موجود في `allowedOrigins` في `server/index.js`

**لو شفت:**
```
404 Not Found
```
**معناها:** السيرفر موجود بس الـ API endpoint غلط
- تأكد إن `VITE_API_URL` مش فيه `/api` زيادة

---

## 🎯 الحلول حسب المشكلة

### المشكلة 1: عندي مشروع واحد فقط على Vercel

**الحل:**
1. لازم ترفع مجلد `server` كمشروع منفصل
2. اتبع الخطوات في `DEPLOY_SERVER_GUIDE.md`

### المشكلة 2: `VITE_API_URL` غلط

**الحل:**
1. روح مشروع الكلاينت → Settings → Environment Variables
2. عدل `VITE_API_URL` للرابط الصحيح
3. Redeploy الكلاينت

### المشكلة 3: CORS Error

**الحل:**
1. افتح `server/index.js` محلياً
2. تأكد إن `https://almurqab.vercel.app` موجود في `allowedOrigins`
3. ارفع التعديل على GitHub
4. Vercel هيعمل Redeploy تلقائي

### المشكلة 4: السيرفر بيرجع 404

**الحل:**
1. شوف Build Logs في Vercel للسيرفر
2. غالباً المشكلة في Root Directory (لازم يكون `server`)
3. أو في Environment Variables ناقصة

---

## 📝 Checklist نهائي

قبل ما تجرب تاني، تأكد من:

**على Vercel:**
- [ ] عندي **مشروعين**: واحد للكلاينت + واحد للسيرفر
- [ ] السيرفر فيه Root Directory = `server`
- [ ] السيرفر فيه `MONGODB_URI` و `JWT_SECRET`
- [ ] الكلاينت فيه `VITE_API_URL` = رابط السيرفر الصحيح
- [ ] لما أفتح رابط السيرفر بيطلع JSON فيه `"status": "ALL DONE ✅"`

**في الكود:**
- [ ] `server/index.js` فيه `https://almurqab.vercel.app` في `allowedOrigins`
- [ ] آخر تعديلات اترفعت على GitHub
- [ ] Vercel عمل Redeploy بعد آخر تعديل

---

## 🆘 لو لسه مش شغال

ابعتلي:
1. **عدد المشاريع** اللي عندك على Vercel
2. **أسماء المشاريع**
3. **رابط Production** لكل مشروع
4. **Screenshot** من Console في المتصفح (F12) وإنت على `https://almurqab.vercel.app`
